name: Check formatting

on:
  pull_request:

permissions:
  contents: write
  pull-requests: write
  checks: write

jobs:
  black:
    name: Python Black
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable

